import styles from "./FormLink.module.scss";

const FormLink = () => {
  return (
    <a className={styles.form_link} href="#">
      <p>Let's Talk</p>
      <i />
    </a>
  );
};

export default FormLink;
